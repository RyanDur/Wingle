require 'spec_helper'

describe "CardWall" do
  describe "GET /card_walls" do
    it "works! (now write some real specs)" do
      get root_path
      response.status.should be(200)
    end
  end
end
